<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Music Shop</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="#">Features</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Pricing</a>
        </li>
      </ul>
      <div class="float-right">      
        <% if user_signed_in? %>
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn btn-sm btn-danger me-2" %>
          <%= link_to current_user.username, edit_user_registration_path, class: "btn btn-sm btn-yello" %>
        <% else %>
          <%= link_to 'Sign In', new_user_session_path, class: "btn btn-sm btn-primary me-2" %>
          <%= link_to 'Sign Up', new_user_registration_path, class: "btn btn-sm btn-success" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>